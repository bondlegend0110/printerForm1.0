import { StlViewer } from "react-stl-viewer";

const Upload = () => {
    return (
        <div className="min-h-screen w-full bg-emerald-700">
            Something
        </div>
    );
};

export default Upload;